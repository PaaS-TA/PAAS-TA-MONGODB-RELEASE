# mongo Database Settings
mongodb.hosts = <%= p('mongodb_broker.hosts')%>
mongodb.port = <%= p('mongodb.port')%>
mongodb.dbName = <%= p('mongodb_broker.db_name')%>
mongodb.userName = <%= p('mongodb.user')%>
mongodb.authSource = <%= p('mongodb_broker.authSource')%>
mongodb.password = <%= p('mongodb.passwd')%>
